{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "echo-ranked-server-bot.serviceAccountName" . }}
  labels:
    {{- include "echo-ranked-server-bot.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
